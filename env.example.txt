# Database
# Get this from your PostgreSQL provider (Neon, Supabase, etc.)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# SendGrid Configuration (Required)
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY="SG.your-sendgrid-api-key-here"

# SendGrid Sender Email (Required)
# Must be verified in SendGrid: https://app.sendgrid.com/settings/sender_auth
FROM_EMAIL="your-verified@email.com"

# SendGrid Sender Name (Optional)
FROM_NAME="Your Name"

# SendGrid Webhook Verification (Optional but Recommended)
# Get this from: https://app.sendgrid.com/settings/mail_settings
# Go to Event Webhooks > Create Webhook > Copy Verification Key
SENDGRID_WEBHOOK_VERIFICATION_KEY="your-webhook-verification-key-here"

